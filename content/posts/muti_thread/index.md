---
title: 多线程编程混乱的核心原理
date: 2026-07-05T09:32:20+08:00
draft: false
tags:
  - 多线程
categories:
  - 多线程
description: 多线程编程混乱的核心原理，多线程编程混乱的核心原理。
---

## 背景
之前多线程编程的时候，大部分都是使用***锁***和***条件变量***实现的临界区保护；有些时候却发现：代码在x86架构上跑的没问题，但是在ARM架构上跑的就不对了。后来才了解到arm架构和x86架构在内存barrier、cache coherency等方面有很大差异。也让我再次深入的了解了一下多线程编程混乱的核心原理。

### 1 . 指令的原子性
这和应该是大家最熟悉的；C/C++某个指令，会拆开成多条指令执行，比如：I++，在x86架构上，会被拆成两条指令;这样就导致在执行一个指令的时候，另外一个线程可以插入进来执行。
### 2. 代码的顺序性
c++的代码默认是从上到下顺序执行的，但是编译器为了提高效率，会将代码进行重排序。CPU为了提高效率，也会对代码进行乱序执行。
```cpp
// 线程 A：程序员写的代码
tail_.store(5, std::memory_order_relaxed);  // ①
flag = true;                                  // ②

// 经过优化或者CPU乱序执行：：② 先执行了
// 线程 B
if (flag) {           // flag == true ✅ （② 先执行完了）
    x = tail_.load(); // 但 tail_ 还是旧值 ❌ （① 还没执行）
}
```
### 3. cache的一致性
现代CPU通常由多个核心组成，每个核心都有自己的cache; 多个线程通常被分配到不同的核心上执行。当一个线程修改了某个变量的值，但是另外一个线程还在使用旧的cache数据，就会导致混乱。(x86架构上，通常不会出现这个问题，因为x86架构的cache一致性比较好。但是ARM架构上就会出现这个问题)

### 4. 编译器的优化
某些变量，编译器会将其优化到寄存器中；这就导致当前变量只有当前线程可见当前线程的修改，其他线程看不到。


## 以上四点，就是导致多线程编程混合的基本原理，具体如何解决，以及我们常用的锁和条件变量是如何解决的，后面会继续分享。或者大家自行查阅相关资料。
